#include <string>
#include <map>

#include "longest_substring.h"
#include "is_anagram.h"
#include "remove_every_other_char.h"

using namespace std;

void sep(string s, std::map<string, int> &c);

int main() {
//    std::cout << longest_substring("nndNfdfdf") << std::endl;
    return 0;
}